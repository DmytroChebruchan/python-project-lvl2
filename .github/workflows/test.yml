name: test
on: push
jobs:
  test:
    runs-on: ubuntu-latest 
    steps:
    -  name: test
       run: 
         pip3 install poetry
         @poetry run python3 -m tests.test_gendiff file1.json file2.json