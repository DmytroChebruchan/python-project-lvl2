name: pytest
on: [push]
jobs:
  pytest:
    runs-on: ubuntu-latest 
    steps:
    -  uses: actions/checkout@v3
    -  name: pytest
       run: |
            pip3 install poetry
            pip install pytest
            poetry run pytest